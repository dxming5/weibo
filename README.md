# Weibo Downloader

一个简单易用的微博图片/视频下载工具，支持批量下载和增量更新。

## 功能特性

- **多类型下载**：支持下载微博图片、Live 图片和视频
- **增量更新**：自动跳过已下载内容，避免重复下载
- **批量管理**：支持同时管理多个博主，一键更新所有博主
- **断点续传**：网络中断后可继续下载，不丢失进度
- **反爬机制**：内置时间间隔设置，降低被封禁风险
- **进度显示**：实时显示下载进度和获取状态

## 目录结构

```
/blog/
├── bloggerlist.txt          # 记录所有博主名称和URL
└── <blogger_name>/          # 每个博主一个独立文件夹
    ├── picture_url.txt      # 已下载图片URL记录
    ├── live_url.txt         # 已下载Live URL记录
    └── video_url.txt        # 已下载视频URL记录
```

## 使用方法

### 运行环境

- Python 3.x
- 无需额外依赖（使用标准库）

### 操作指令

| 指令 | 功能 |
|------|------|
| `a` | 新增博主下载 |
| `b` | 更新所有博主的图片 |
| `c` | 单独更新指定博主 |
| `d` | 删除指定博主及下载内容 |
| `e` | 从指定博主开始更新后续所有博主 |
| `l` | 查看博主列表 |
| `x` | 退出程序 |

### 下载路径配置

在代码中修改 `set_total_path()` 函数：

```python
download_dir = 'F:/图片/blog/'  # 改为你的目标路径
```

## 版本历史

| 版本 | 更新内容 |
|------|----------|
| v1 | 基础多页图片获取 |
| v2 | 优化页面URL获取函数 |
| v3 | 优化有效页面数获取 |
| v4 | 优化图片保存函数 |
| v5 | 支持从最后一页开始存储、记录下载数量 |
| v6 | 修复重连后存储异常，添加URL去重记录 |
| v7 | 修复连接断开后命名异常 |
| v8 | 去除页概念，支持全部URL获取和超时设置 |
| v9 | 单页获取失败时仅重试当前页 |
| v10 | 分离图片和视频下载 |
| v11 | 重构图片URL获取，支持Live图片 |
| v12 | 功能优化 |
| v13 | 以微博时间为单位记录 |
| v14 | 限制最大下载条数（~500条） |
| v15 | 添加时间间隔功能（反爬机制） |

## 配置选项

在 `weibo.py` 中可调整以下配置：

```python
blogger_max_num = 0        # 单博主最大下载条数（0为不限制）
is_time_interval = True    # 是否启用时间间隔
```

## 免责声明

本工具仅供学习交流使用，请勿用于商业目的或违反微博服务条款的行为。
